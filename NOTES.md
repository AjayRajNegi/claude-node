#### Generator function, yield, iterator function

- With iterator function we can define our own iterations. Generator functions make it easy and yield holds value for a specific iteration
- Here it is used to iterate over the stream(chunks) which will be received from the LLM.

#### Error Handling

- Exponential Backoff
